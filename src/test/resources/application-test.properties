# Test profile overrides - prevent real SMTP connection during tests
# Disable Spring Boot's MailSender connection validation which attempts to connect to the configured SMTP server
spring.mail.test-connection=false

# Provide harmless/dummy mail settings so code that reads them won't fail
spring.mail.host=localhost
spring.mail.port=25
spring.mail.username=dummy
spring.mail.password=dummy

