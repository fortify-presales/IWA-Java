name: "Empty CodeQL scan by excluding all paths"
disable-default-queries: false           # leave defaults ON so the CLI has queries
paths-ignore:
  - "**"                                 # ignore everything