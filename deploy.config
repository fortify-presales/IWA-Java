# deploy.config - default values for deploy.ps1
# INI-style sections are supported: [azure] and [docker]
# Configuration precedence: Script Parameters > Environment Variables > deploy.config
# When a parameter appears under a section, the script will first check for an environment
# variable named SECTION_PARAM (e.g. AZURE_ACR_NAME or DOCKER_IMAGE_NAME) before falling
# back to generic env vars (ACR_NAME) or the config value.
#
# SECURITY: Avoid storing secrets in this file when possible. Use environment variables or a
# CI secret store for sensitive values (AcrPass etc.). The fields AcrUser and AcrPass are
# intentionally left empty — prefer setting AZURE_ACR_USER and AZURE_ACR_PASS in your CI or
# environment instead of committing them here.

[azure]
# Azure resource group name
ResourceGroup=
# Azure Container Registry name (short name, without ".azurecr.io")
AcrName=
# Azure Web App name
AppName=iwapharmacydirect
# Azure location
Location=useast
# App Service plan name
PlanName=
# App Service plan SKU
Sku=B2
# Optional: supply ACR credentials (overrides automatic retrieval). Use env vars for secrets when possible.
AcrUser=
AcrPass=

[docker]
# Local image name (repository portion). Example: iwa or myorg/iwa
ImageName=iwa
# Image tag to use
Tag=latest
# Whether to build before pushing (true/false)
Build=false
# Path to Dockerfile to use when building
Dockerfile=Dockerfile

# Application settings to set on the Azure Web App (key=value format)
[env]
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=
SPRING_MAIL_PASSWORD=
SPRING_MAIL_DEFAULT_ENCODING=UTF-8
SPRING_MAIL_SMTP_AUTH=true
SPRING_MAIL_SMTP_STARTTLS_ENABLE=true
SPRING_MAIL_TEST_CONNECTION=false
SPRING_MAIL_DEBUG=false
WEB_PORT=8080